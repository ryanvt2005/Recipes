# Production Environment Configuration
# IMPORTANT: Never commit actual secrets to version control!

# Database Configuration
# Use the private endpoint from your Lightsail Managed Database
# Format: ls-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.xxxxxxxxxxxx.us-east-1.rds.amazonaws.com
DB_HOST=your-lightsail-managed-db-private-endpoint
DB_PORT=5432
DB_NAME=recipeapp
DB_USER=recipeuser
DB_PASSWORD=USE_STRONG_PASSWORD_FROM_SECRETS_MANAGER
DB_SSL=true

# Application Configuration
NODE_ENV=production
PORT=3000
FRONTEND_URL=https://your-production-domain.com

# Authentication (Use strong random secret from secrets manager)
JWT_SECRET=USE_STRONG_SECRET_FROM_SECRETS_MANAGER

# External APIs
ANTHROPIC_API_KEY=USE_API_KEY_FROM_SECRETS_MANAGER

# Logging
LOG_LEVEL=warn
