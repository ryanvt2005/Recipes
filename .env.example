# Database Configuration
# For local development: use 'localhost' (or 'db' if using docker-compose with local postgres)
# For production: use your Lightsail Managed Database private endpoint
DB_HOST=localhost
DB_PORT=5432
DB_NAME=recipeapp
DB_USER=recipeuser
DB_PASSWORD=changeme
# Set to 'true' for Lightsail Managed Database (requires SSL); leave unset for local dev
DB_SSL=false

# Application Configuration
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:3001

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this

# External APIs
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Optional: Logging Level (error, warn, info, debug)
LOG_LEVEL=info
